@AGENTS.md

# Sweem reference

`ref-client-test.md` is the canonical reference for this demo — deployed mainnet IDs, the
sweem-server API, the on-chain Move functions (targets + arg orders), the client lib
(`src/lib/{sweem,tx,api}.ts`), and the org + employee flows. **Read it before changing or
porting any Sweem logic.** It is the spec to re-implement these flows in the production
frontend (`fe/`).

@ref-client-test.md
